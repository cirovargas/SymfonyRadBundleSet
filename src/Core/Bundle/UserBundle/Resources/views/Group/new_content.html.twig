{% trans_default_domain 'FOSUserBundle' %}

    
    <div class="page-header">
    <h1>Adicionar grupo</h1>
</div>
<div class="row">
    <div class="col-sm-12">
        {#<form class="panel form-horizontal" action="{{ path('fos_user_group_new') }}" method="POST" {{ form_enctype(form) }}>#}
            {{ form_start(form,{'attr':{'class':'panel form-horizontal' } }) }}
            <div class="panel-body">
                {{ form_rest(form) }}
               
            </div>
            <div class="panel-footer text-right">
                                <a class="btn" href="{{ path('fos_user_group_list') }}">
    {{ 'views.recordactions.backtothelist'|trans({}, 'CoreAdminBundle') }}
</a>
                
                <button type="submit" class="btn btn-success">{{ 'group.new.submit'|trans }}</button>
            </div>
        {{ form_end(form) }}
    </div>
</div>