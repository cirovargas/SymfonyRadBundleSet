{% trans_default_domain 'FOSUserBundle' %}
   
    
<div class="page-header">
    <h1>Editar grupo</h1>
</div>
<div class="row">
    <div class="col-sm-12">
        {% form_theme form 'MopaBootstrapBundle:Form:fields.html.twig' %}
        <form class="panel form-horizontal" action="{{ path('fos_user_group_edit', {'groupName': group_name} ) }}" method="POST" {{ form_enctype(form) }}>
            <div class="panel-body">
                {{ form_widget(form) }}
               
            </div>
            <div class="panel-footer text-right">
                                <a class="btn" href="{{ path('fos_user_group_list') }}">
    {{ 'views.recordactions.backtothelist'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}
</a>
                
                <button type="submit" class="btn btn-success">{{ 'group.edit.submit'|trans }}</button>
                
                
            </div>
        </form>
    </div>
</div>

  